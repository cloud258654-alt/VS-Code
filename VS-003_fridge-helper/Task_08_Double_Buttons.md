任務目標：為了避免數量較多時需要連續點擊的困擾，請將食材卡片右側的「吃完了」單一按鈕，拆解為「吃 1 個」與「全吃完」雙按鈕並排設計。

請修改現有模組，執行以下重構：

1. 修改 `js/storage.js`：
   - 保留上一階段的 `consumeFood(id)`（負責扣 1 個或扣 10% 的邏輯）。
   - 新增一個函數 `clearFood(id)`：
     - 內部邏輯：根據傳入的 UUID (id) 找到對應的食材物件。
     - 直接將其 `quantity` 歸零 (0)，並將 `finished` 屬性設為 `true`。
     - 將更新後的陣列儲存回 LocalStorage。

2. 修改 `js/ui.js` 中的卡片 HTML 渲染邏輯（`renderApp`）：
   - 找到原本渲染「吃完了」按鈕的地方，將其替換為一個並排的按鈕組（Flex 排版）：
     - **按鈕 A (吃 1 個 / 用一點)**：
       - 文字：若為個數模式顯示「吃 1 個」；若為醬料百分比模式顯示「用 10%」。
       - 事件：綁定點擊調用 `storage.consumeFood(id)`。
       - 視覺：粉紅邊框、白色底的圓潤小按鈕。
     - **按鈕 B (全吃完 / 空罐了)**：
       - 文字：顯示「全吃完」或「空罐了」。
       - 事件：綁定點擊調用 `storage.clearFood(id)`。
       - 視覺：粉紅底、白色字的小按鈕。
   
3. 優化 `js/ui.js` 中的動效與渲染：
   - 點擊「吃 1 個 / 用 10%」：維持原地 Q 彈與愛心特效，若數量歸零才淡出。
   - 點擊「全吃完 / 空罐了」：立刻觸發強烈的粉紅愛心粒子特效（heartParticles），並讓整張卡片直接執行淡出動畫，500 毫秒後移入底部的「✔ 已完食」區。

4. 修改 `js/main.js`：
   - 確保在總指揮官中正確處理這兩個新按鈕事件的橋接。

請確保雙按鈕在手機畫面上排列整齊、不會超出版面，並維持 Hello Kitty 的日系可愛風情。