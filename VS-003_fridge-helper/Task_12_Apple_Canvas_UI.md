任務目標：將「冰箱保衛戰」全面重構成具備 Apple 官網美學的極簡高質感介面。主畫面與卡片改用 HTML5 Canvas 進行動態無縫渲染，追求極致流暢的動態效果、細緻線條與蘋果經典的「空氣感」排版。

請執行以下重構與修改：

1. 視覺風格規範 (Apple Premium Style)：
   - 底色：改為極致純淨的蘋果淺灰色背景 (#F5F5F7) 與純白 (#FFFFFF)。
   - 文字：採用極致現代的黑灰色 (#1D1D1F) 與優雅的副標題灰色 (#86868B)。
   - 移除所有卡通化、高飽和度的色彩與複雜標籤，改用 1px 的細緻灰色線條與超大圓角 (rounded-2xl) 堆疊出層次感。
   - 號誌燈優化：
     - 🚨 救命呀 $\rightarrow$ 改為 Apple 警示紅 (#FF3B30) 的 1px 微光外框。
     - 🐱 快吃我 $\rightarrow$ 改為 琥珀橘 (#FF9500) 的極細文字標籤。
     - 🎀 安全唷 $\rightarrow$ 改為 經典薄荷綠 (#34C759) 的呼吸小圓點。

2. 核心技術重構：引入 `js/canvas-ui.js`：
   - 在主畫面中央建立一個全螢幕（或寬度自適應）的 `HTML5 Canvas` 畫布，接管原本由 HTML 渲染的四大區塊（快過期、快吃我、安全、已完食）。
   - 所有的食材卡片、血條進度條、剩餘天數文字，全部改由 Canvas 的 `ctx.fillText`、`ctx.roundRect` 與 `ctx.stroke` 進行高解析度（支援 Retina 螢幕的 2x 縮放）繪製。
   - 存放位置圖示改為極簡的幾何線條圖示（冷藏、冷凍、常溫）。

3. 絲滑動態特效 (Apple Motion Library)：
   - **卡片登場與完食移出：** 點擊按鈕時，不要死板消失，在 Canvas 裡面使用「彈簧物理公式 (Spring Physics Algorithm)」來計算位移。點擊「全吃完」時，卡片會像 iOS 刪除 App 一樣，帶著輕微縮小並優雅地向下滑入已完食區，伴隨極淡的晶瑩水滴散開效果（取代原本的愛心）。
   - **血條更新：** 當點擊「用了一點」時，Canvas 的進度條會呈現絲滑的線性漸變減短動畫，沒有任何鋸齒。

4. 雙按鈕優化：
   - 在 Canvas 中精準偵測點擊區域（滑鼠/手指座標）。按鈕改為蘋果經典的隱形膠囊外觀，平常只有淡淡的灰色字，懸停時滑順浮現微陰影。

請確保原本的多單位 (ml/g)、自訂存放位置、LocalStorage 的核心邏輯完全保留，純粹用 Canvas 將 UI 質感與流暢度拉升至 Apple 官網級別。